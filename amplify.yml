version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Front
        - npm ci
    build:
      commands:
        - npm run build -- --configuration production
  artifacts:
    baseDirectory: Front/dist/front
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
